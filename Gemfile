source "https://rubygems.org"

# Only needed to build the site in CI. GitHub Pages builds the published
# site itself and ignores this file; _config.yml excludes it from output.
gem "jekyll", "~> 4.3"
gem "kramdown-parser-gfm", "~> 1.1"
